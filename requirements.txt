# Core dependencies for Akita-Zmodem-MeshCore

# For MeshCore network communication
# Please ensure the installed 'meshcore' library is compatible with the API used in the script
# (e.g., fdlamotte/meshcore_py or a similar asyncio-based library with 'create_serial', 'subscribe', 'commands.send_msg')
meshcore

# Zmodem: implemented in-tree (zmodem.py). No external `zmodem` package
# is required for normal operation. If you need a full-featured third-party
# implementation, install it explicitly and update this file.
# zmodem  # optional

# Optional, for CLI progress bars
tqdm

# Optional: CRC utilities (not required unless using external zmodem libs)
crcmod
